## Conditional Block

### Syntax

_if expr_

```rust
if <keyword> expr {

} ...arms
```

_match expr_

```rust
match <keyword> expr {
  ...arms
}
```

**optional `<keyword>`**

- `const`
   marks expression as constant i.e not observable.
